<%- include('./layouts/header') %>
<%- include('./menu') %>

<div class="book-wrapper">
    <div class="book">
        <h1><%= book.title %></h1>
        <p>Описание: <%= book.description %></p>
        <p>Автор: <%= book.authors %></p>
        <% if (book.favourite === true) { %>
            <p>В избранном: да</p>
        <% } %>

        <% if (book.favourite === false) { %>
            <p>В избранном: нет</p>
        <% } %>

        <% if (book.fileCover) { %>
            <p><%= book.fileCover %></p>
        <% } %>

        <% if (viewData.inc && viewData.inc > 0) { %>
            <p>Количество просмотров: <%= viewData.inc %></p>
        <% } %>

        <% if (!viewData.inc || viewData.inc === 0) { %>
            <p>Количество просмотров: 0</p>
        <% } %>

        <a class="edit" href="/books/<%= book.id %>/edit">Редактировать</a>

        <a class="delete" href="/books/<%= book.id %>/delete">Удалить книгу</a>
    </div>

    <a class="return" href="javascript:history.back()">Вернуться</a>
</div>

<%- include('./layouts/footer') %>
